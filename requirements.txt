streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
altair>=5.0.0
requests>=2.31.0
openpyxl>=3.1.0
pytest>=7.4.0

- name: Install dependencies
  run: |
    python -m pip install --upgrade pip
    pip install pytest
    if [ -f requirements.txt ]; then pip install -r (ref)](https://github.com/VitExMachina/Wearable_Sensor_/blob requirements.txt; fi
